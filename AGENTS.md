# Agents

## 代码规范

### 风格

- 不要做任何兼容性修改。
- 永远保持代码简洁。

### 测试

- 如果因为更改项目代码而导致测试不通过，需要修复测试代码。
  - 测试为项目代码服务，而非项目代码为测试服务。

### 修改完文件

- 如果修改对象是 `pkg/**/*.go` 文件, 执行 `go test <相关测试文件>` 运行相关测试。
- 如果修改对象是代码文件, 为代码添加或者更新doc-string, 并保持与代码逻辑一致。
- 除非特意要求, 不必同步更改docs/下的相应文档。

### 关于 git

- 可以查看 git 区内容
- 但任何情况下不要修改我的 git 区
